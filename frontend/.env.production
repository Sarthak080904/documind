# Baked into the bundle at build time by Vite. Not a secret — it is the public
# address of the API, visible in every network request the page makes.
# Change this if the backend moves, then redeploy the frontend.
VITE_API_URL=https://documind-api-xiuy.onrender.com
